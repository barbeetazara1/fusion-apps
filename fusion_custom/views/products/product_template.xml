<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="fusion_custom_product_template_form">
            <field name="name">product.template.fusion.custom.form</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='general_information']" position="after">
                    <page name="product_inventory" string="Inventory Cost">
                        <field name="product_inventory_line" widget="one2many">
                            <tree editable="bottom">
                                <field name="product_id" options="{'no_open': True, 'no_create': True}"/>
                                <field name="qty"/>
                                <field name="uom_id" options="{'no_open': True, 'no_create': True}"/>
                            </tree>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>